pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "Building" 
                bat "mvn clean install -DskipTests"
            }
        }
        stage('Test') { 
            steps {
                echo "Testing" 
                bat "mvn test"
            }
        }
        stage('Deploy') { 
            steps {
                echo "Deploying" 
		bat "spring-boot:build-image"
            }
        }
    }
}
