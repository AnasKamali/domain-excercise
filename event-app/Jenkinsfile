pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "Building" 
                bash "mvn clean install -DskipTests"
            }
        }
        stage('Test') { 
            steps {
                echo "Testing" 
                bash "mvn test"
            }
        }
        stage('Deploy') { 
            steps {
                echo "Deploying" 
		bash "spring-boot:build-image"
            }
        }
    }
}
